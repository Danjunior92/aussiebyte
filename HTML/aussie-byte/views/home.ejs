<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Aussie Byte</title>
  <link rel="stylesheet" href="/style.css">
</head>

<body>
  <!-- Header: site title and link to create a new post -->
  <header>
    <%- include('partials/header.ejs') %>
  </header>

  <main>
    <!-- The server passes `posts` (an array) to this template.
         We iterate over `posts` and render each as an article. -->
    <% posts.forEach(post => { %>
      <article>
        <!-- Link to the single-post page for this post -->
        <a href="/post/<%= post.id %>">
          <h2><%= post.title %></h2>
        </a>
        <!-- Post body/content -->
        <p><%= post.content %></p>
      </article>
    <% }) %>
  </main>

  <footer>
    <%- include('partials/footer') %>
  </footer>
</body>

</html>